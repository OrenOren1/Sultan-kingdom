apiVersion: v1
kind: Service
metadata:
  name: {{ include "visitore.fullname" . }}
  labels:
    {{- include "visitore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "visitore.selectorLabels" . | nindent 4 }}
