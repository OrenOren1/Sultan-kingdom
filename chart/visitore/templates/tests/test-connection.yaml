apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "visitore.fullname" . }}-test-connection"
  labels:
    {{- include "visitore.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "visitore.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
